SRC = ../src
PROGRAMS := $(patsubst $(SRC)/%.cpp,%,$(wildcard $(SRC)/*.cpp)) 
PROGRAMS := $(filter-out main,$(PROGRAMS))

all:
	$(MAKE) -C ..

clean:
	$(MAKE) -C .. clean

$(PROGRAMS): %:
	$(MAKE) -C .. $@
