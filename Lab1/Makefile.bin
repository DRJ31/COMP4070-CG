SRC = ../src
PROGRAMS := $(patsubst $(SRC)/%.cpp,%,$(wildcard $(SRC)/*.cpp)) 
PROGRAMS := $(filter-out main,$(PROGRAM))

all:
	@echo $(PROGRAMS)
	$(MAKE) -C ..

clean:
	$(MAKE) -C .. clean

$(PROGRAMS): %:
	$(MAKE) -C .. $@
